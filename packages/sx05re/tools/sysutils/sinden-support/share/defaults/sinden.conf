# EmuELEC Sinden Lightgun configuration
#
# autostart: start the Sinden runtime automatically when a game begins
# keep_running: keep the runtime alive after exiting a game (1=yes)
# hotplug_autostart: automatically start when the gun is connected via udev

autostart=1
keep_running=0
hotplug_autostart=1
