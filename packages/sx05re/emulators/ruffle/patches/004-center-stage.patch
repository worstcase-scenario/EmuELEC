diff --git a/src/main.rs b/src/main.rs
index b99cf2a..7e73de7 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -26 +26,3 @@ use ruffle_core::tag_utils::SwfMovie;
-use ruffle_core::{Player, PlayerBuilder, PlayerEvent, StageScaleMode, ViewportDimensions};
+use ruffle_core::{
+    Player, PlayerBuilder, PlayerEvent, StageAlign, StageScaleMode, ViewportDimensions,
+};
@@ -517,0 +520,3 @@ pub fn main() {
+        // Centre the stage even if the movie asks for a corner alignment,
+        // otherwise the black bars end up on one side only.
+        .with_align(StageAlign::empty(), true)
